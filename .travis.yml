language: minimal

stages:
  - build
  - deploy

jobs:
  include:
    - stage: build
      script: echo "doing nothing"

    # deploy stage doesn't define own script: and runs
    # default implicit "script: bash -c 'echo $BASH_VERSION'"
    - stage: deploy
